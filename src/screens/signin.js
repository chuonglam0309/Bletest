import { StyleSheet,
    Text,
    View,
    NativeModules,
    NativeEventEmitter,
    Platform,
    PermissionsAndroid,
    Button, 
    FlatList,
    Alert,
    TouchableOpacity,
    ScrollView,
    SafeAreaView 
} from 'react-native'
import React, { useEffect, useState } from 'react'

const Signin = (props) => {
return(
    <SafeAreaView>
        <Text>Sign in</Text>
    </SafeAreaView>
)
}
export default Signin

